%YAML:1.0

NvDCF:
  useUniqueID: 1    # Use 64-bit long Unique ID when assignining tracker ID.
  maxTargetsPerStream: 99
  
  # Tracker performance settings
  trackerCore:
    minTrackingConfidenceDuringInactive: 0.5
    minTargetObjectConfidence: 0.2
    minTargetObjectBboxArea: 20
    maxInactiveFrames: 30
