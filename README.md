# Deepstream SDK with 4 Camera Tracking

This project is a boilerplate for tracking objects from 4 camera streams using Nvidia DeepStream SDK.

## Structure
- `configs/`: Configuration files for DeepStream application, inference, and tracker.
- `src/`: Source code for custom implementations.
- `include/`: Header files.
- `scripts/`: Helper scripts for running the app.
